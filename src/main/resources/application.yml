micronaut:
  application:
    name: thoughtsporadic-be
  server:
    cors:
      enabled: true
      configurations:
        web:
          allowedOrigins:
            - http://blog.thoughtsporadic.com
          allowedMethods:
            - GET
            - POST
          exposedHeaders:
            - Content-Type
            - Cache-Control
            - Content-Language
            - Content-Length
            - Expires
            - Last-Modified
      localhost-pass-through: true
netty:
  default:
    allocator:
      max-order: 3
